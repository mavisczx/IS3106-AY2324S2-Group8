/*
 *
 * This is the Page dealing with the full extended version of 
 * each thread, including thread title, thread description  and all 
 * the posts in each thread and all the likes eachpost has, it also gives
 * the compressed version of a sub thread which when clicked on 
 * opens up into another "Full thread"
 * 
 */

import React, { useState } from "react";
import { Icon } from "@iconify/react";
import ApiThread from "../../helpers/ApiThread";
import { Link } from "react-router-dom";


const fullThread= (0 => {

 const[post, setPost] = useState();
 const[thread, setThread] = useState();

// const methods
// return  
  
};

export default fullThread;
